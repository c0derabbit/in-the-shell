<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1" />
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no user-scalable=no">
    <link rel="stylesheet" type="text/css" href="{{asset "style.css"}}" />

    <title>{{meta_title}}</title>

    {{ghost_head}}
  </head>

  <body class="{{body_class}}">
    <header>
      <h1>
        <a href="{{@blog.url}}">
          {{#if @blog.logo}}
            <img src="{{@blog.logo}}" width="100" />
          {{/if}}
          {{@blog.title}}
        </a>
      </h1>
    </header>

    <main>
      {{{body}}}
    </main>

    <div class="footer-wrapper">
      <footer>
        <nav>
          {{#if @blog.logo}}<img class="footer-logo" src="{{@blog.logo}}" />{{/if}}
          <ul>
            <li><a href="{{@blog.url}}/rss/">Subscribe</a></li>
          </ul>
        </nav>
        <div class="footer-bottom">
          <p>
            Theme by <a href="https://c0derabbit.github.io" target="_blank">c0derabbit</a>. 
            Built with the amazing <a href="https://ghost.org" target="_blank">Ghost</a>.
          </p>
        </div>
      </footer>
    </div>
    {{ghost_foot}}
  </body>
</html>
